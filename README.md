# wuwa_gacha_sim

鸣潮（Wuthering Waves）**「角色活动唤取」限定 5★ 角色**抽卡模拟器，Rust 命令行程序。

给定抽卡次数 `N`，模拟这一段抽卡过程并输出统计：5★/4★ 产出、限定与歪的次数、
平均出金抽数、以及**大珊瑚（余波珊瑚）的完整结算明细**。

```console
$ cargo run --release -- -n 200 --seed 42
```

只模拟限定角色池，不含武器池、常驻池与新手池。

---

## 1. 编译与运行

需要 Rust 1.85 或更高（本项目使用 edition 2024，开发环境为 rustc 1.92）。

```bash
cargo build --release
./target/release/wuwa_gacha_sim -n 200 --seed 42
# 或者
cargo run --release -- -n 200 --seed 42
```

## 2. 命令行参数

| 参数 | 说明 |
| --- | --- |
| `-n, --pulls <N>` | **必填**，抽卡次数，`N >= 0` |
| `--seed <U64>` | 基准随机种子；不给则取系统时间，并打印实际使用的种子 |
| `--light` | 精简输出：每次模拟只打印一行「限定 / 常驻 5★ 数量」，与 `--repeat` 搭配使用 |
| `--repeat <K>` / `--loop <K>` | 独立重复模拟 `K` 次，`K >= 1`，默认 `1` |
| `--summary-only` | 只打印汇总与结束状态，不打印逐条明细 |
| `--detail` | 强制打印逐条明细（`N > 200` 时默认不打印） |
| `--no-color` | 关闭 ANSI 颜色 |

明细阈值：`N <= 200` **且** `--repeat 1` 时默认打印明细，否则默认只出汇总。
颜色在「stdout 是终端」且未设置环境变量 `NO_COLOR` 时才启用。

非法输入（负数、非数字、未知参数、缺少 `-n`、`--repeat 0`、`--detail` 与 `--summary-only` 同时给出）
一律打印错误到 `stderr` 并以**退出码 2** 结束；正常运行为 `0`。

程序**不接受任何初始状态参数**，也**没有续跑或状态持久化**：每次运行、每次重复
都从「5★/4★ 保底均为 0 抽、非大保底」的全新状态开始。

### 2.1 `--repeat`：重复模拟

`--repeat K` 把整场模拟独立跑 `K` 次，**不做任何跨次汇总**，就是把同一套统计重复输出 `K` 遍。

第 `i` 次（从 1 开始）使用的种子是 `基准种子 + i - 1`，因此：

- 每一次都是全新的保底状态，各次之间互不影响；
- 结果可复现：`--seed 42 --repeat 3` 的第 2 次，等价于单独跑 `--seed 43`。

完整模式下每次重复前会打印一行分节标题：

```
── 第 1/2 次  seed=42 ────────────────────────
```

#### 为什么「种子 +1」不会让各次结果趋同

这是个合理的担心：**如果 RNG 把种子直接当状态用**（比如朴素 LCG），
那么种子 42 和 43 会产生高度重叠的序列，`--repeat` 出来的结果就会假性一致。

ChaCha8 不会这样，原因有两层：

1. **u64 种子不直接当密钥。** `rand_core` 的 `seed_from_u64` 先用 PCG32 的输出函数
   把 u64 搅拌成 32 字节密钥（`state → state × MUL + INC` 再异或右移、循环移位）。
   种子 42 与 43 进去之后，中间状态相差的不是 1 而是 `MUL`（一个 64 位大奇数）。
   该函数的文档明确写着它「designed such that low Hamming Weight numbers like 0 and 1
   can be used and should still result in good, independent seeds」。
2. **ChaCha8 是密码学流密码。** 密钥变一位，8 轮 ARX 扩散后输出完全雪崩。

实测（`cargo run --release --example seed_quality`，26 万个 u64 样本）：

```
种子相差       +1 : 平均 Hamming 距离 31.9987 位   卡方 14.60 (df=9, 临界 16.92)  ✓
种子相差  +65_536 : 平均 Hamming 距离 32.0005 位   卡方  9.23 (df=9, 临界 16.92)  ✓
种子相差    +2^32 : 平均 Hamming 距离 32.0029 位   卡方 13.06 (df=9, 临界 16.92)  ✓
种子相差    +2^63 : 平均 Hamming 距离 31.9918 位   卡方  9.93 (df=9, 临界 16.92)  ✓
对照组（同一种子自比）: 平均 Hamming 距离 0.0000 位  ← 检验灵敏度确认
```

「+1」和「+2^63」的表现没有区别，都等同于随机。

模拟层再看一次（20000 次 × 200 抽，三种派生方式对比）：

```
派生方式                          均值     方差      范围    P(限定=0)
基准种子 + i （--repeat 的方式）   2.0545   0.7675   1..7      0.00%
大跨度种子 i × 黄金比常数          2.0663   0.7743   1..7      0.00%
单个主 RNG 派生                    2.0671   0.7665   1..7      0.00%

连续种子 vs 主 RNG 派生 的卡方: 5.08 (df=6, 临界 12.59)  ✓ 通过
```

均值彼此相差不到 0.7%，方差几乎相同。**结论：「+1」是安全的。**

> 顺带解释 `P(限定=0)` 为什么是 0：单次出金的间隔最多 80 抽，所以 200 抽里
> **至少**会出 2 个 5★，限定数不可能为 0。

需要注意的另一点：`--repeat` 派生出的是一串**确定性**序列，不是「真随机」。
「真随机」只体现在基准种子取自系统时间（或你手动指定的任意值）。
只要基准种子够任意，整组结果就够任意；而同一组种子永远可复现——这正是想要的取舍。

### 2.2 `--light`：精简输出

`--light` 每次模拟只输出一行，只含限定与常驻 5★ 的数量（外加该次使用的种子，便于复现）：

```console
$ ./target/release/wuwa_gacha_sim -n 200 --seed 42 --light --repeat 10
=== 鸣潮 · 角色活动唤取 模拟 · light ===
抽卡次数 : 200
基准种子 : 42
重复次数 : 10

#1    限定 1  常驻 1  seed=42
#2    限定 4  常驻 1  seed=43
#3    限定 5  常驻 0  seed=44
#4    限定 4  常驻 1  seed=45
#5    限定 1  常驻 2  seed=46
#6    限定 2  常驻 2  seed=47
#7    限定 2  常驻 1  seed=48
#8    限定 1  常驻 2  seed=49
#9    限定 1  常驻 2  seed=50
#10   限定 1  常驻 2  seed=51
```

计数排在前面、种子排在后面，这样不同位数的种子不会把前面的列推歪。
`--light` 下 `--detail` / `--summary-only` 没有意义（明细本来就不输出）。

**`--light` 不会改变随机流消耗**：它只影响渲染，同一个种子在 `--light` 与完整模式下
得到的计数完全一致。这一条有测试守着（`light_agrees_with_full_output_for_the_same_seed`）。

## 3. 概率模型

### 3.1 5★ 出率曲线

设 `n` = 距上次出 5★ 已抽的抽数 + 1（即本次是第 `n` 抽）：

```
p5(n) = 1.0                            if n >= 80
      = min(1.0, 0.008 + 0.06*(n-65))  if n >= 66
      = 0.008                          otherwise (n <= 65)
```

| n | 1 | 65 | 66 | 79 | 80 |
| --- | --- | --- | --- | --- | --- |
| `p5(n)` | 0.8% | 0.8% | 6.8% | 84.8% | 100% |

> **这不是官方公式。** 官方只公示了「基础 0.8% + 综合 1.8% + 至多 80 抽」三个数，
> 没有披露软保底曲线的形状。上面这条曲线是一个能**同时复现这三个数**的拟合模型
> （综合出率算出来是 1.8648%，与公示的 1.8% 相符）。

### 3.2 4★ 出率曲线

设 `pity4` = 已累计未出「4★ 或以上」内容的抽数：

```
p4(n) = 1.0     if n >= 10
      = 0.06    otherwise
```

本模拟器**不实现** 4★ 的 50/50、不实现 4★ 大保底、不区分 UP / 非 UP，4★ 只按概率产出。
但会把 **4★ 角色**与 **4★ 武器**分开统计，因为两者给的大珊瑚不同（8 vs 3，见第 4 节）：
出 4★ 后以 `FOUR_STAR_WEAPON_SHARE` 的概率判定为武器。

**判定顺序与重置语义（关键）：**

1. 一次唤取只产出一个物品：先判 5★，未出 5★ 再判 4★，仍未出则产出 3★ 武器。
2. **产出 5★ 时把 `pity4` 归零**。官方措辞是「至多 10 次唤取必获得至少 1 个 4★ **或以上**内容」，
   5★ 同样满足这个保底条件，所以它也会重置 4★ 计数。
3. 产出 4★ 时 `pity4` 归零；否则 `pity4 += 1`。

> 第 2 条不是可有可无的细节。两种实现在 4★ 出率上相差 **0.64 个百分点**：
>
> | 实现 | 4★ 出率 |
> | --- | --- |
> | 5★ 重置 `pity4`（本项目采用） | **12.12%** |
> | 5★ 不重置 `pity4` | 12.76% |
>
> `src/tests.rs` 里的 `pity4_is_reset_by_five_star` 专门锁住这条语义，
> 大样本测试的 4★ 上界也卡在 12.5%，一旦漏实现就会测崩。

### 3.3 5★ 的 50/50 与大保底

- 非大保底状态下，5★ 有 **50%** 为 UP 限定角色、**50%** 为常驻 5★角色。
- 若出常驻（即「歪」），则**下一次** 5★ 必定为 UP 限定角色。
- 出金后保底计数归零，大保底标记相应更新。

## 4. 大珊瑚（余波珊瑚）结算

「大珊瑚」是社区俗称，游戏内货币名为**余波珊瑚**，8 个可兑换 1 抽。

| 产出 | 大珊瑚 | 构成 |
| --- | --- | --- |
| 5★ **限定**角色 | **+15** | 基础 15 |
| 5★ **常驻**角色（歪） | **+45** | 基础 15 **+ 歪补偿 30** |
| 4★ **角色** | **+8** | 基础 3 **+ 满共鸣链转化 5** |
| 4★ **武器** | **+3** | 武器没有共鸣链，固定 3，不会涨到 8 |
| 3★ 武器 | **+0** | 走另一种货币「小珊瑚」，本程序不统计 |

因此恒有：

```
大珊瑚总量 == 限定5★数 × 15 + 常驻5★数 × 45 + 4★角色数 × 8 + 4★武器数 × 3
```

这条恒等式是**结构性成立**的（珊瑚只在 `Stats::record` 里按产出物品结算），
并有单元测试 `coral_identity_holds_over_a_large_run` 守着。

### 4.1 建模假设

1. **假设所有 4★ 角色均已满共鸣链**，故每个 4★ 角色按 8 计。
   4★ 武器固定 3，不受此假设影响。
2. **不计算 5★ 的满命加成**。真实规则中 5★ 角色第 8 次及以后重复获得给 40；
   本模拟器不做角色重复计数，限定 5★ 一律按 15 计。
3. **4★ 里武器的占比是实测值，不是官方数据**，见 4.2。

### 4.2 4★ 武器占比：实测值

官方公示只说了「4★ 内容基础概率 6.0%，其中 **50%** 为任一 UP 角色（角色池每期 3 个）」，
对**非 UP 的那 50% 里角色与武器各占多少从未公开**，攻略站也只笼统写「4★ 武器/4★ 角色」。

所以这个值是用 `tools/gacha_history.py`（见第 11 节）实测的。
一位玩家「角色精准调谐」（限定角色池）6 个月共 310 抽的记录：

| 项目 | 数值 |
| --- | --- |
| 4★ 样本 | 38 个 |
| ├ 角色 | 33 |
| └ 武器 | **5** |
| 武器占比点估计 | **13.2%** |
| Wilson 95% 置信区间 | **[5.8%, 27.3%]** |

因此 `FOUR_STAR_WEAPON_SHARE = 0.13`。样本量仍偏小、区间较宽，
多收集记录可以收窄（改 `src/gacha.rs` 顶部这一个常量即可）：

| `FOUR_STAR_WEAPON_SHARE` | 每个 4★ 平均珊瑚 | 每 100 抽大珊瑚 |
| --- | --- | --- |
| 0.00（全部当角色） | 8.00 | 143.6 |
| **0.13（当前实测值）** | **7.35** | **135.7** |
| 0.25（先前按均分猜的） | 6.75 | 128.4 |
| 0.50（角色武器各半） | 5.50 | 111.3 |

大样本测试会断言模拟出的武器占比收敛到这个常量，所以改错了会立刻被测出来。

另外，3★ 武器产出的「小珊瑚」（残振珊瑚，70 个兑换 1 抽）**完全不统计、不输出**。

## 5. 统计口径

- **出金率** = 5★ 总数 ÷ 总抽数；**平均出金抽数** = 总抽数 ÷ 5★ 总数。
- **常驻 5★(歪)** 就是输掉 50/50 的次数，两者**恒为同一个数**：大保底状态下出金必定是限定，
  所以「拿到常驻 5★」与「输掉 50/50」互为充要条件。既然永远相同，只显示一行。
- 输出**不展示 3★ 武器数量**（它在实际中不产生任何有用资源）。
  程序内部仍在统计它，单元测试用它确认三档稀有度把总抽数分干净了。
- 除数为 0 时（如 `N = 0`）显示 `-`，不会出现 `NaN` / `inf`，也不会 panic。
- 明细行的 `#` 序号按事件发生顺序对 5★ 与 4★ 连续编号。
- 输出顺序固定为：汇总 → 大珊瑚明细 → 结束状态 → 与理论值对比 → **明细（最后）**。

## 6. 理论值

### 6.1 长期平稳值

模拟次数足够多之后收敛到的数值，也是大样本校验的断言基准。
5★ 部分为精确解析解；4★/3★ 与珊瑚为 2000 万抽参考模拟的实测值。

| 指标 | 理论值 |
| --- | --- |
| 5★ 综合出率 | 1.865% |
| 期望出金抽数 | 53.625 抽 |
| 限定 5★ 出率 | 1.243%（占 5★ 的 2/3），期望 80.44 抽/限定 |
| 常驻 5★ 出率 | 0.622%（占 5★ 的 1/3） |
| 65 抽内出金概率 | 40.67% |
| 5★ 分布 | P(X≤66)=44.7%、P(X≤70)=79.6%、P(X≤75)=99.3% |
| 4★ 出率 | 12.12%（期望 8.25 抽/4★） |
| 3★ 武器出率 | 86.01% |
| 大珊瑚 / 100 抽 | 135.7（含实测的 4★ 武器占比，见 4.2）|

三项出率之和恒为 100%：1.865% + 12.12% + 86.01% ≈ 100%。

### 6.2 从零保底起步的精确期望

**上面那些长期值不能直接乘抽数用。** 一次真实的抽卡是从「保底 0 抽」开始的，
前 65 抽享受不到软保底，所以短抽数的实际期望明显更低：

| 抽数 | E[5★] | E[限定] | E[4★] | 折算 5★ 率 |
| --- | --- | --- | --- | --- |
| 80 | 1.258 | 0.689 | 9.5 | 1.57% |
| 200 | **3.251** | **2.058** | 24.1 | 1.63% |
| 1000 | 18.248 | 12.054 | 121.0 | 1.82% |
| 2000 | 36.896 | 24.486 | 242.2 | 1.84% |
| 长期 | — | — | — | 1.865% |

200 抽时两者的差距不小：长期口径给 `200 × 1.865% = 3.73` 个 5★，
而真实期望只有 **3.25** 个；限定数的差距更大（2.49 vs 2.06，虚高 21%）。
拿长期口径去评价运气会**系统性地偏严**，让人误以为自己是非酋。

所以程序里的「与理论值对比」和所有 `[理论 …]` 标注都用**精确期望**，而不是长期率。
它由 `theory::expectations(pulls)` 动态规划求出：状态是
`(剩余抽数, pity5, 是否大保底, pity4)`，逆推该状态下的期望「限定 / 常驻 / 4★」数量。

- `pulls <= 2000`：直接递推，精确。
- `pulls > 2000`：只精确算 2000 步，其余用平稳期每抽增量线性外推
  （多算 100 步来估计该增量）。误差在 `1e-12` 量级。

实测交叉验证（`examples/seed_quality.rs`，20000 次独立模拟 × 200 抽）：

```
                        限定        常驻     5★ 合计         4★
精确 DP               2.0582     1.1928     3.2510    24.1308
实测（20000 次平均）   2.0545     1.1941     3.2485    24.1450
长期口径 抽数 × 出率    2.4864     1.2432     3.7296    24.2414
```

## 7. 输出示例

```console
$ ./target/release/wuwa_gacha_sim -n 200 --seed 42 --no-color
=== 鸣潮 · 角色活动唤取 模拟 ===
随机种子 : 42
抽卡次数 : 200

── 汇总 ──────────────────────────────────────
5★ 总数          : 2
  限定 5★        : 1
  常驻 5★(歪)    : 1
  综合出金率     : 1.00%   [理论 1.63%]
  平均出金抽数   : 100.00  [理论 61.52]

4★ 总数          : 24
  4★ 出率        : 12.00%  [理论 12.07%]
  平均每 4★ 抽数 : 8.33    [理论 8.29]

── 大珊瑚（余波珊瑚）明细 ────────────────────
总获取量         : 237
  限定 5★        : 1 × 15 =   15
  常驻 5★(歪)    : 1 × 45 =   45    (15 基础 + 30 歪补偿)
  4★ 角色        : 21 × 8 =  168    (3 基础 + 5 满链转化)
  4★ 武器        : 3 × 3 =    9    (武器没有共鸣链，固定 3 个)
平均每抽         : 1.19    [理论 1.31]
平均每 100 抽    : 118.5   [理论 131.0]

── 结束状态 ──────────────────────────────────
5★ 保底计数      : 58/80（距硬保底还差 22 抽）
下次 5★          : 大保底（必为限定）
4★ 保底计数      : 1/10（距 4★ 保底还差 9 抽）

── 与理论值对比 ──────────────────────────────
期望 5★ 总数     : 3.25   实际 2      (偏差 -38.5%)
期望限定 5★      : 2.06   实际 1
期望 4★ 总数     : 24.1   实际 24
期望大珊瑚       : 261.9   实际 237

── 明细 ──────────────────────────────────────
#1  第   10 抽   4★  武器   +3  大珊瑚
#2  第   20 抽   4★  内容   +8  大珊瑚
...
#8  第   73 抽   5★  限定   +15 大珊瑚   (50/50 成功)
...
#17 第  142 抽   5★  常驻   +45 大珊瑚   (50/50 失败 → 下次必限定)
...
```

明细放在**最后**：它最多 200 行，放前面会把汇总挤下去。

200 抽只出 2 金属于正常的运气波动——`期望 5★ 总数 3.25` 是均值，
单次 200 抽的方差本来就很大。

## 8. 测试

```bash
cargo test                                    # 单元测试 + CLI 集成测试
cargo clippy --all-targets -- -D warnings     # 静态检查
cargo test --release -- --ignored --nocapture # 200 万抽大样本校验
```

大样本校验（2,000,000 抽，seed = 7）的实测结果：

```
5★ 出率        : 1.8645%  (理论 1.8648%)
平均出金抽数   : 53.6337   (理论 53.625)
4★ 出率        : 12.0964%  (理论 12.1207%)
3★ 武器出率    : 86.0391%
平均每抽大珊瑚 : 1.3545   (理论 1.3571)
平均每 100 抽  : 135.4
4★ 武器占比    : 0.1296  (设定 0.13)
```

覆盖范围：

- **`src/tests.rs`**：概率曲线的取值与单调性、5★ 硬保底、`pity4` 被 5★ 重置、
  5★ 大保底、大珊瑚恒等式、可复现性、`N = 0` 边界、理论值自洽性，
  以及 `theory::expectations` 的精确期望与实测均值一致、外推分界处无跳变。
- **`tests/cli.rs`**：拉起真实二进制，验证退出码 2 的各类非法输入、
  明细阈值、`--no-color`、同 seed 输出完全一致、续跑参数确实不存在、
  「明细必须排在最后」「已删除的几行确实不在输出里」，
  以及 `--light` 与完整模式结果一致、`--repeat` 的重复次数与种子递增、`--loop` 别名等价。
- **`examples/seed_quality.rs`**：种子独立性的比特级与模拟层检验，
  外加精确期望与实测的对照。用 `cargo run --release --example seed_quality` 运行。

## 9. 可复现性

随机源是 `rand_chacha::ChaCha8Rng`，通过 `SeedableRng::seed_from_u64` 播种。

**没有**使用 `rand::rngs::StdRng`：它的算法不保证跨 rand 版本稳定，
一旦升级依赖，同一个种子就会给出不同结果。ChaCha8 的输出由算法规范
完全确定，与平台和版本无关。

`Cargo.toml` 里的依赖用 `=` 精确锁版本，且 `Cargo.lock` 已提交，
因此「同一 `--seed` 得到同一结果」是可靠的。

## 10. 目录结构

```
Cargo.toml / Cargo.lock
src/
  main.rs     CLI 解析与中文渲染
  lib.rs      库入口
  gacha.rs    概率模型、保底状态机、大珊瑚常量（所有常量只在这里定义一次）
  rng.rs      可复现随机源
  stats.rs    统计聚合
  tests.rs    单元测试
tests/
  cli.rs      命令行集成测试
examples/
  seed_quality.rs   种子独立性检验 + 精确期望对照
tools/
  gacha_history.py  抓取并分析你自己的唤取记录（见第 11 节）
```

## 11. 用你自己的抽卡记录校准 4★ 武器占比

第 4.2 节里的 `FOUR_STAR_WEAPON_SHARE = 0.25` 是个猜的值。想换成实测值，
可以用 `tools/gacha_history.py` 把你自己账号的唤取记录拉下来统计。

### 11.1 隐私

抽卡链接里的 `record_id` 等同于读取你抽卡记录的凭证。这个脚本的设计前提是
**它只在你本机跑，凭证不经过任何第三方**：

- 请求只发给库洛官方接口（`gmserver-api.aki-game2.com` / `.net`）；
- **不打印、不落盘** URL 或任何 ID；
- 除非显式加 `--dump`，否则不保存原始记录。`--dump` 出来的文件含账号字段，
  已在 `.gitignore` 中排除，但仍请自行保管。

所以：**不要把抽卡链接发给我**，把脚本的输出摘要发给我就够了。

### 11.2 用法

```bash
# 从游戏里「唤取记录」页面取链接（PC 端在 Client.log 里搜 aki-gm-resources.aki-game.com）
python3 tools/gacha_history.py "<抽卡链接>"

# 顺便把原始记录存下来（链接只有约 1 小时有效期，存下来可以反复分析）
python3 tools/gacha_history.py --dump /tmp/ww.json "<抽卡链接>"
python3 tools/gacha_history.py --from-dump /tmp/ww.json
```

脚本会输出三块内容：

1. **各卡池概览** —— 每个卡池的记录数、4★ 角色/武器拆分、5★ 数量。
2. **4★ 武器占比** —— 含 Wilson 95% 置信区间，并换算出「每个 4★ 平均给几个大珊瑚」。
3. **5★ 出金间隔分布** —— 用来反向校验模拟器的软保底曲线：
   把「≤65 抽出金」的实测比例与模型的 40.67% 对比。

### 11.3 常见报错

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| `链接里缺少参数：resources_id, record_id, …`，且提示里列出的参数名带反斜杠或为空 | 终端把 URL 里的 `?` `=` `&` `#` 转义成了 `\?` `\=` `\&` `\#`（zsh 的 `url-quote-magic` 之类的行编辑插件会这样），反斜杠原样进了参数 | 脚本会自动清掉反斜杠；若仍失败，把链接用**单引号**包起来再试 |
| 所有卡池都返回 0 条记录 | 链接过期（有效期约 1 小时），接口此时**静默返回空数组而不报错** | 重新打开游戏内的唤取记录页面，再取一次链接 |
| `无法识别的链接域名` | 链接不是唤取记录页 | 国服域名应为 `aki-gm-resources.aki-game.com` |

### 11.4 已实测的结果

用本工具统计一位玩家 6 个月的唤取记录，得到两个结论：

**① 4★ 武器占比 = 13.2%**（限定角色池，310 抽，38 个 4★，其中 5 个武器）。
已写入 `FOUR_STAR_WEAPON_SHARE`，详见 4.2。置信区间 [5.8%, 27.3%] 仍宽，
样本再多一些可以收窄。

**② 5★ 出金数 4 个 / 310 抽 —— 与模型一致，没有证据说明软保底曲线有问题。**

这一点值得展开，因为它容易被误读成「我太非了，肯定是池子有问题 / 模型算错了」：

| | |
| --- | --- |
| 模型期望 `E[N(310)]` | 5.28 个（长期口径 `310/53.6 = 5.78`）|
| 标准差 | 1.09 |
| `P(N ≤ 4)` | **21.5%** |
| `P(N = 4)` | 21.5% |
| 实际上限 | 出金间隔最多 80 抽，所以 310 抽**至少**出 4 个 —— 这是理论下界 |

也就是说「310 抽只出 4 金」大约 **5 个人里就有 1 个**会碰到，是正常波动，
完全在模型的预测范围内。真要检验软保底曲线，需要的是**很多个完整出金间隔的平均值**，
而不是单次抽数的总数：这位玩家的 3 个完整间隔是 `[59, 70, 77]`，
`P(三间隔之和 ≤ 206) = 84.5%`，也属于正常范围。

结论：**当前模型（0.8% 基础 + 66 抽起每抽 +6% + 80 抽硬保底）没有被数据推翻。**
要真正验证它，需要把多个玩家的记录合并起来统计出金间隔分布。

### 11.5 能指望多少精度

6 个月的记录通常只有几十个 4★，置信区间半宽大约 ±10 个百分点。
这足以**排除极端假设**（比如「4★ 全是角色」或「角色武器各半」），
但定不死到小数点后一位。样本越多越好。

脚本里还有两处需要你知道的近似：

- 只能统计**两端都落在记录窗口内**的出金间隔，所以首个间隔被排除
  （6 个月的窗口是从中间截断的，第一个 5★ 之前有多少保底是未知的）。
- 卡池识别靠启发式：取「5★ 全是角色且 5★ 名字种类最多」的池子当限定角色池。
  如果认错了，脚本会提示你人工判断。
