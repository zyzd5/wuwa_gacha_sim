# 程序生成提示词（v0.4）

> **用法**：把「=== 提示词开始 ===」到「=== 提示词结束 ===」之间的全部内容，作为一次性需求说明交给编码代理，
> 生成整个 Rust 项目。
>
> **v0.3 → v0.4 变更**：
> 1. **新增「歪补偿」规则**：抽到常驻 5★（歪）时，除基础 15 外额外补 **30**，共 **45** 大珊瑚。
> 2. **4★ 默认视为已满共鸣链**：每个 4★ 按 **8** 个大珊瑚计（基础 3 + 共鸣链转化 5），不再按 3 计。
> 3. **修正一处我自己的建模错误**：5★ 产出会重置 4★ 保底计数。此前误按「不重置」计算，
>    导致 4★ 出率写成 12.76%；正确值应为 **12.12%**。这个差异有 0.64 个百分点，不可忽略，必须按重置实现。
> 4. **（后续修订）4★ 拆分为角色与武器**：4★ 角色给 8 大珊瑚、4★ 武器给 3，
>    两者必须分开统计；比例由 `FOUR_STAR_WEAPON_SHARE` 决定（官方未公示，取 0.25）。见 3.4 节。

---

=== 提示词开始 ===

你是一名资深 Rust 工程师。请在本目录下从零实现一个**鸣潮（Wuthering Waves）「角色活动唤取」限定 5★ 角色抽卡模拟器**的命令行程序。

## 1. 交付物

1. Rust binary crate，名为 `wuwa_gacha_sim`。
2. `cargo run --release -- -n 200` 可直接运行并输出统计。
3. `cargo test` 全部通过；`cargo build --release` 与 `cargo clippy -- -D warnings` 均无警告。
4. `README.md`（中文）：编译/运行方式、命令行参数、**完整的概率模型与珊瑚结算说明（含第 3.4 节的两条建模假设）**、理论值表。
5. 结构：

   ```
   Cargo.toml
   Cargo.lock          # 提交，保证可复现
   README.md
   src/main.rs         // CLI 解析 + 结果渲染
   src/gacha.rs        // 卡池状态机：5★/4★ 保底计数、5★ 大保底、概率函数、珊瑚常量
   src/rng.rs          // 可复现随机源封装
   src/stats.rs        // 统计聚合（含大珊瑚结算）
   src/tests.rs 或 tests/gacha.rs
   ```

## 2. 技术约束

- Rust edition 2024，`rustc 1.92` 可编译。
- 允许第三方依赖：`clap`（derive 特性）+ `rand` + `rand_chacha`。请在 `Cargo.toml` 中用**精确版本**（如 `=4.5.x` / `=0.9.x`）并在 README 说明。
- 随机源用 `rand_chacha::ChaCha8Rng`，通过 `SeedableRng::seed_from_u64(seed)` 播种。
  **不要**用 `rand::rngs::StdRng`——它的算法不保证跨 rand 版本稳定，会破坏「同 seed 同结果」的承诺。
- 不给 `--seed` 时用系统时间纳秒做种子，并**把实际使用的种子打印出来**，方便复现。
- 非法输入不得 panic：错误打到 `stderr`，退出码 `2`。正常运行退出码 `0`。
- 所有概率与珊瑚常量只在 `gacha.rs` 顶部集中定义一次，禁止散落魔法数字。
- 不联网、不读配置文件、不使用 async/tokio。
- **不做任何状态持久化，不做续跑/断点恢复**：每次运行都从「保底 0 抽、非大保底」的全新状态开始。
- 中文输出；支持 `--no-color` 关闭 ANSI 颜色。

## 3. 概率模型（必须严格按此实现）

### 3.1 5★ 出率曲线

设 `n` = 「距上次出 5★ 已抽的抽数」+ 1，即本次是第 `n` 抽：

```
p5(n) = 1.0                            if n >= 80
      = min(1.0, 0.008 + 0.06*(n-65))  if n >= 66
      = 0.008                          otherwise (n <= 65)
```

检查点：`p5(1)=0.8%`、`p5(65)=0.8%`、`p5(66)=6.8%`、`p5(79)=84.8%`、`p5(80)=100%`。

代码用 `pity5`（已累计未出 5★ 的抽数，取值 `0..=79`）作状态，本次抽取用 `p5(pity5 + 1)`。

> 说明：官方只公示了「基础 0.8% + 综合 1.8% + 至多 80 抽」三个数，未披露软保底曲线。
> 上面的曲线是一个能同时复现这三个数的拟合模型，README 里要这么写，不要谎称是官方公式。

### 3.2 4★ 出率曲线

设 `pity4` = 已累计未出「4★ 或以上」内容的抽数（取值 `0..=9`），本次用 `p4(pity4 + 1)`：

```
p4(n) = 1.0     if n >= 10
      = 0.06    otherwise
```

**本模拟器不实现 4★ 的 50/50、不实现 4★ 大保底、不区分 UP / 非 UP**，4★ 只按上述概率产出。
但要把 **4★ 角色**与 **4★ 武器**分开统计（两者给的大珊瑚不同，见 3.4）：
出 4★ 后以 `FOUR_STAR_WEAPON_SHARE` 的概率判定为武器。

判定的顺序与交互（**这里的重置语义必须照做，它会显著改变结果**）：

- 一次唤取只产出一个物品：先判 5★，未出 5★ 再判 4★，仍未出则产出 3★ 武器。
- **产出 5★ 时把 `pity4` 归零**。官方措辞是「至多 10 次唤取必获得至少 1 个 4★ **或以上**内容」，
  即 5★ 同样满足这个保底条件，因此重置计数。
- 产出 4★ 时 `pity4` 归零；否则 `pity4 += 1`。

> **不要**把这条改成「5★ 不重置 `pity4`」。两种实现在 4★ 出率上相差 **0.64 个百分点**
> （重置 12.12% ↔ 不重置 12.76%），不是可以随意选择的细节。必须实现为「重置」。

### 3.3 5★ 的 50/50 与大保底

维护布尔状态 `guaranteed5`：

- `true` → 本次 5★ **必定**是 UP 限定角色，出金后置 `false`；
- `false` → 本次 5★ 有 50% 为 UP 限定角色、50% 为常驻 5★角色；若出常驻（即「歪了」）则置 `true`。

**没有** `guaranteed4`，也没有任何 4★ 侧的保底状态。

### 3.4 大珊瑚（余波珊瑚）结算

每次唤取按产出物品结算游戏内货币**余波珊瑚**（社区俗称「大珊瑚」，8 个可兑换 1 抽）：

| 产出 | 大珊瑚 | 构成 |
| --- | --- | --- |
| 5★ **限定**角色 | **+15** | 基础 15 |
| 5★ **常驻**角色（即「歪」） | **+45** | 基础 15 **+ 歪补偿 30** |
| 4★ **角色** | **+8** | 基础 3 **+ 满共鸣链转化 5** |
| 4★ **武器** | **+3** | 武器没有共鸣链，固定 3，不会涨到 8 |
| 3★ 武器 | **+0** | — |

常量集中定义：

```rust
pub const CORAL_5STAR_LIMITED: u32 = 15;
pub const CORAL_5STAR_STANDARD: u32 = 45;   // = 15 基础 + 30「歪」补偿
pub const CORAL_4STAR_CHARACTER: u32 = 8;   // = 3 基础 + 5（假设已满共鸣链）
pub const CORAL_4STAR_WEAPON: u32 = 3;      // 武器无共鸣链，固定 3
pub const CORAL_PER_PULL_EXCHANGE: u32 = 8; // 8 个珊瑚换 1 抽
pub const FOUR_STAR_WEAPON_SHARE: f64 = 0.13; // ⚠️ 官方未公示；0.13 是实测值
```

**恒等式**（必须用一个单元测试锁死）：

```
大珊瑚总量 == 限定5★数 × 15 + 常驻5★数 × 45 + 4★角色数 × 8 + 4★武器数 × 3
```

**必须写进 README 的建模假设**（不要静默处理）：

1. **假设所有 4★ 角色均已满共鸣链**，故每个 4★ 角色按 8 计；4★ 武器固定 3。
2. **不计算 5★ 的满命加成**。真实规则中 5★ 角色第 8 次及以后重复获得给 40；
   本模拟器不做角色重复计数，限定 5★ 一律按 15 计。
3. **4★ 里武器的占比没有官方依据**（官方只说「50% 为 UP 角色」，非 UP 那半的构成未公开）。
   取值来自 `tools/gacha_history.py` 对真实唤取记录的实测统计：38 个 4★ 中 5 个武器，
   点估计 13.2%（Wilson 95% CI [5.8%, 27.3%]），故取 `FOUR_STAR_WEAPON_SHARE = 0.13`。
   README 必须写明这是实测值、样本量与置信区间。

**明确不实现**：

- 5★ 满命后的 40 珊瑚档位、角色重复计数。
- 3★ 武器产出的另一种副产物「小珊瑚」（残振珊瑚，70 个兑换 1 抽）：**完全不统计、不输出**。
- 珊瑚商店的兑换交互：只做「可兑换抽数」的折算展示，不做购买模拟。

### 3.5 理论值（自检基准）

5★ 部分为精确解析计算；4★/3★ 与珊瑚数据由 2000 万抽蒙特卡洛实测得出：

| 指标 | 理论值 |
| --- | --- |
| 5★ 综合出率 | **1.865%** |
| 期望出金抽数 | **53.625** 抽 |
| 限定 5★ 出率 | **1.243%**（占 5★ 的 2/3），期望 **80.44 抽/限定** |
| 常驻 5★ 出率 | **0.622%**（占 5★ 的 1/3） |
| 65 抽内出金概率 | 40.67% |
| 5★ 分布参考 | P(X≤66)=44.7%、P(X≤70)=79.6%、P(X≤75)=99.3% |
| 4★ 出率 | **12.12%**，期望 **8.25 抽/4★** |
| 3★ 武器出率 | **86.01%** |
| 大珊瑚 / 抽 | **1.436** |
| 大珊瑚 / 100 抽 | **143.6** |
| 大珊瑚 / 200 抽 | **287.2** |

> 三项出率之和恒为 100%：1.865% + 12.12% + 86.01% ≈ 100%。

### 3.6 明确不在本次范围

- 武器池、常驻池、新手池。
- 星声 / 月相 / 抽卡券消耗。
- 4★ 的 UP / 非 UP 判定、4★ 大保底、具体的 4★ 角色与武器名。
- 5★ 满命加成、角色重复计数、任何跨运行的状态持久化。

## 4. 命令行接口

```
wuwa_gacha_sim -n <N> [选项]
```

| 参数 | 说明 |
| --- | --- |
| `-n, --pulls <N>` | **必填**，抽卡次数，`N >= 0` |
| `--seed <U64>` | 随机种子；缺省用系统时间并打印实际种子 |
| `--summary-only` | 只打印汇总与结束状态，不打印逐条明细 |
| `--detail` | 强制打印逐条明细 |
| `--no-color` | 关闭 ANSI 颜色 |

明细打印策略：`N <= 200` 默认打印，`N > 200` 默认不打印（`--detail` 可强制，`--summary-only` 可禁止）。
用 `clap` 的 derive + `value_parser` 做校验；非法值（负数、非数字、未知参数、缺 `-n`）→ stderr + 退出码 2。

## 5. 输出格式

### 5.1 正常输出

```
=== 鸣潮 · 角色活动唤取 模拟 ===
随机种子 : 42
抽卡次数 : 200

── 汇总 ─────────────────────────────────────
5★ 总数          : 4
  限定 5★        : 3
  常驻 5★(歪)    : 1
  歪(50/50 失败) : 1 次
  综合出金率     : 2.00%   [理论 1.86%]
  平均出金抽数   : 50.00   [理论 53.63]

4★ 总数          : 24
  4★ 出率        : 12.00%  [理论 12.12%]
  平均每 4★ 抽数 : 8.33    [理论 8.25]

3★ 武器          : 172    (86.00%)

5★ 最欧 / 最非   : 12 抽 / 74 抽

── 大珊瑚（余波珊瑚）明细 ───────────────────
总获取量         : 282
  限定 5★        : 3 × 15 =  45
  常驻 5★(歪)    : 1 × 45 =  45    (15 基础 + 30 歪补偿)
  4★（按满链）   : 24 × 8 = 192    (3 基础 + 5 满链转化)
平均每抽         : 1.41    [理论 1.44]
平均每 100 抽    : 141.0   [理论 143.6]
可兑换限定抽数   : 35 抽   (282 ÷ 8，向下取整)

── 明细 ─────────────────────────────────────
#1  第  74 抽   5★  限定     +15 大珊瑚   (歪后大保底)
#2  第  12 抽   5★  常驻     +45 大珊瑚   (50/50 失败 → 下次必限定)
#3  第  15 抽   4★  内容     +8  大珊瑚
#4  第  18 抽   4★  内容     +8  大珊瑚
#5  第  61 抽   5★  限定     +15 大珊瑚   (50/50 成功)
#6  第  68 抽   5★  限定     +15 大珊瑚   (50/50 成功)
...

── 结束状态 ─────────────────────────────────
5★ 保底计数 : 3/80（距硬保底还差 77 抽）
下次 5★     : 非大保底（50/50）
4★ 保底计数 : 7/10（距 4★ 保底还差 3 抽）

── 与理论值对比 ─────────────────────────────
期望 5★ 总数 : 3.73   实际 4      (偏差 +7.2%)
期望限定 5★  : 2.49   实际 3
期望 4★ 总数 : 24.2   实际 24
期望大珊瑚   : 287.2  实际 282
```

> 「结束状态」只是本次运行末尾的纯展示信息，程序**不接受**任何初始状态参数，也没有续跑功能。

### 5.2 数值格式

- 百分比两位小数，抽数两位小数，大珊瑚为整数。
- 除数为 0 时显示 `-`，**不得**出现 `NaN` / `inf` / panic（例如 `N = 0`、`N = 1` 无 4★、可兑换抽数为 0）。
- 明细行的 `#` 序号按事件发生顺序连续编号（5★ 与 4★ 混排）。

## 6. 测试要求

必须包含（名称自定，断言用第 3.5 节数字）：

1. **5★ 曲线**：`p5(1)=p5(65)=0.008`、`p5(66)=0.068`、`p5(79)=0.848`、`p5(80)=1.0`，且在 `1..=80` 上单调不减。
2. **4★ 曲线**：`p4(1..=9)=0.06`、`p4(10)=1.0`；`pity4=9` 时本次抽取必出 4★ 或以上。
3. **5★ 硬保底**：`pity5=79` 时本次抽取必出 5★，且出金后 `pity5` 归零。
4. **5★ 大保底**：`guaranteed5=true` 时出金必为限定，且出金后置 `false`；`false` 时若出常驻则置 `true`。
5. **`pity4` 重置**：产出 5★ 后 `pity4 == 0`（这一条专门锁住 3.2 节的重置语义）；产出 4★ 后 `pity4 == 0`。
6. **大珊瑚结算**：对固定序列或模拟结果断言
   - `大珊瑚总量 == 限定5★数 × 15 + 常驻5★数 × 45 + 4★数 × 8`
   - 只有 3★ 武器时大珊瑚总量为 0
   - 单次「歪」相对「限定」恰好多出 30
7. **可复现性**：同一 `seed` + 同一参数，两次运行的核心统计完全一致（逐字段相等）。
8. **边界**：`-n 0` 正常输出、退出码 0；`-n -1` 退出码 2；`--pulls abc` 退出码 2。
9. **大样本校验**（标 `#[ignore]`，README 写清如何 `cargo test -- --ignored` 运行）：
   跑 2,000,000 抽，断言
   - 5★ 出率 ∈ `[1.83%, 1.90%]`，平均出金抽数 ∈ `[52.5, 54.8]`
   - 4★ 出率 ∈ `[11.8%, 12.5%]`（注意上界要卡在 12.5%，**不是** 13%；若跑出 12.7% 说明 3.2 节的重置漏实现了）
   - 平均每抽大珊瑚 ∈ `[1.40, 1.48]`

## 7. 完成后请自查并在回复中贴出

- `cargo build --release`、`cargo clippy -- -D warnings`、`cargo test` 的真实输出。
- `cargo run --release -- -n 200 --seed 42` 的真实输出。
- `cargo run --release -- -n 2000000 --seed 7 --summary-only` 的真实输出，并与第 3.5 节理论值逐项对比。

## 8. 禁止事项

- 不要用 `StdRng`（跨版本不稳定）、不要用简单 LCG、不要用 `rand::random()` 绕过种子。
- 不要实现 4★ 的 UP / 非 UP、4★ 大保底、5★ 满命加成 —— 这些是第 3.6 节明确排除的内容。
- 不要把 3.2 节的「5★ 重置 `pity4`」改成不重置。
- 不要读写任何状态文件，不要实现续跑、断点恢复或初始状态参数。
- 不要把概率/珊瑚常数复制到多处，不要在渲染层重算概率。
- 不要为了凑任何公示数值而硬编码或修正结果。

=== 提示词结束 ===

---

## 珊瑚规则（你补充的两条，已确认）

| 产出 | 大珊瑚 | 说明 |
| --- | --- | --- |
| 5★ 限定 | 15 | 基础档 |
| 5★ 常驻（歪） | **45** | 15 基础 **+ 30 歪补偿** |
| 4★ | **8** | 3 基础 **+ 5**，前提是「默认 4★ 已满共鸣链」 |
| 3★ 武器 | 0 | 走另一种货币「小珊瑚」，本程序不统计 |

你给的两条规则我都查到了佐证，你的说法成立：
常驻 5★「除了基础的 15 之外，系统还会额外再补偿 30 个余波珊瑚，相当于一共到手 45 个」——
[921 手游网《鸣潮抽金给多少珊瑚》](https://www.921sfw.com/jxsygl/212471.html)；
4★ 满共鸣链后每次 8 个 —— [3DM《鸣潮》抽卡资源介绍](https://shouyou.3dmgame.com/gl/533776.html)。

## 我上一版的一个建模错误（已在本版修正）

v0.1 的验证脚本里，我在「5★ 重置 4★ 保底计数」那一支**只重置了一个没被使用的辅助变量，`pity4` 本身并没有被重置**，
于是把「5★ 不重置」分支的 12.76% 当成了正确值，还错误地在提示词里写「这个选择影响小于 0.01 个百分点，不必纠结」。
重跑后两者相差 **0.64 个百分点**：

| 实现方式 | 4★ 出率 | 大珊瑚/抽 |
| --- | --- | --- |
| 5★ 重置 `pity4`（正确，官方「4★或以上」措辞） | **12.12%** | 1.436 |
| 5★ 不重置 `pity4`（我此前的错误值） | 12.76% | 1.488 |

v0.4 已按正确语义（重置）改定，并在第 3.2 节和第 6 节第 9 条各加了一道防线。

## 顺带解决的一个悬案

v0.2 里我曾报告「官方 4★ 综合概率 12.0% 与『6% + 10 抽保底』矛盾（应为 13.0%）」。
那个结论是上面那个 bug 的产物：把 5★ 重置 4★ 计数考虑进去后，4★ 单项出率正好是 **12.12%**，
与官方公示的 **12.0%** 在四舍五入范围内吻合。也就是说**官方数值本身是自洽的**，
本模拟器的 4★ 口径也与公示一致。你选择「按 6% + 10 抽保底实现」没有问题。

## 已确认的细节（无待定项）

1. **大珊瑚 = 余波珊瑚**：8 个兑换 1 抽；`CORAL_PER_PULL_EXCHANGE = 8`。
2. **「结束状态」保留**：纯展示。程序不接受任何初始状态参数，没有续跑功能。
3. **小珊瑚（残振珊瑚）完全不输出**：不统计、不在结果里出现。
4. **不做 4★ 的 UP / 非 UP 与 4★ 大保底**，4★ 只计数。

## 事实依据

- 官方概率公示原文（5★ 0.8%/综合 1.8%/80 抽保底、50/50 与歪后必 UP、4★ 内容 6.0%/综合 12.0%/10 抽保底、3★ 武器 93.2%）—— [游侠网《鸣潮》抽卡唤取概率规则汇总](https://gl.ali213.net/html/2024-5/1411009.html)。
- 珊瑚档位与歪补偿 —— [921 手游网《鸣潮抽金给多少珊瑚》](https://www.921sfw.com/jxsygl/212471.html)、[3DM《鸣潮》抽卡资源介绍](https://shouyou.3dmgame.com/gl/533776.html)。
- 硬保底 80、软保底约 66 抽起、50/50、保底跨池继承 —— [HostedGG：Convene 保底机制](https://hostedgg.com/wiki/wuwa/convene-pity-system)。
- 「期望约 80 抽/限定、最坏 160 抽」—— [Smart Calculators 鸣潮唤取计算器](https://smart-calculators.net/zh-CN/tools/wuthering-waves-convene-calculator)。
- 软保底自 66 抽起、每抽 +6% 的社区实测拟合（NGA 数据分析帖，需登录）：[tid=40194871](https://bbs.nga.cn/read.php?tid=40194871)。
- 「大珊瑚 = 余波珊瑚」是社区俗称，官方未使用「大珊瑚」一词，README 需写明。
- 珊瑚另有用途（限定 5★ 共鸣链 360 珊瑚 / 常驻 5★ 270 珊瑚，每个角色限购 2 次），本次**不实现**，
  若日后想加，只需在汇总里加一行折算。
